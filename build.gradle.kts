/**
 * ===============================================================================
 * 功能：StringFogPro 根构建配置。
 * 函数简介：仅声明供子模块使用的 AGP 插件版本（apply false），不在根工程直接应用。
 * ===============================================================================
 */

plugins {
    // AGP 9.2.1：与上游 VirtualApp 主工程一致，供 :stringfog 子模块应用。
    id("com.android.library") version "9.2.1" apply false
}
